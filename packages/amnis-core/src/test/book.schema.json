{
  "$id": "book",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "StateCreate": {
      "type": "object",
      "properties": {
        "book": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Book"
          }
        }
      },
      "additionalProperties": false
    },
    "Book": {
      "type": "object",
      "properties": {
        "$id": {
          "$ref": "#/definitions/Identifier",
          "description": "Identifier for this entity.",
          "default": ""
        },
        "created": {
          "$ref": "#/definitions/DateJSON",
          "description": "Creation date string.",
          "default": ""
        },
        "updated": {
          "$ref": "#/definitions/DateJSON",
          "description": "Updated date string.",
          "default": ""
        },
        "committed": {
          "type": "boolean",
          "description": "Flag to determine if the entity has been committed to storage.",
          "default": false
        },
        "delete": {
          "type": "boolean",
          "description": "If this entity is marked to be deleted.",
          "default": false
        },
        "$owner": {
          "$ref": "#/definitions/Identifier",
          "description": "Entity that owns this data."
        },
        "$readers": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Identifier"
          },
          "description": "Entities that can observe this data. Pseudo-owners of this data, but only as a reader."
        },
        "$creator": {
          "$ref": "#/definitions/Identifier",
          "description": "Possible user id creator of the entity."
        },
        "$children": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Identifier"
          },
          "description": "Children of this entity."
        },
        "title": {
          "type": "string"
        },
        "price": {
          "type": "number"
        }
      },
      "required": [
        "$creator",
        "$id",
        "$owner",
        "$readers",
        "committed",
        "created",
        "delete",
        "price",
        "title",
        "updated"
      ],
      "additionalProperties": false
    },
    "Entity": {
      "type": "object",
      "properties": {
        "$id": {
          "$ref": "#/definitions/Identifier",
          "description": "Identifier for this entity.",
          "default": ""
        },
        "created": {
          "$ref": "#/definitions/DateJSON",
          "description": "Creation date string.",
          "default": ""
        },
        "updated": {
          "$ref": "#/definitions/DateJSON",
          "description": "Updated date string.",
          "default": ""
        },
        "committed": {
          "type": "boolean",
          "description": "Flag to determine if the entity has been committed to storage.",
          "default": false
        },
        "delete": {
          "type": "boolean",
          "description": "If this entity is marked to be deleted.",
          "default": false
        },
        "$owner": {
          "$ref": "#/definitions/Identifier",
          "description": "Entity that owns this data."
        },
        "$readers": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Identifier"
          },
          "description": "Entities that can observe this data. Pseudo-owners of this data, but only as a reader."
        },
        "$creator": {
          "$ref": "#/definitions/Identifier",
          "description": "Possible user id creator of the entity."
        },
        "$children": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Identifier"
          },
          "description": "Children of this entity."
        }
      },
      "required": [
        "$id",
        "created",
        "updated",
        "committed",
        "delete",
        "$owner",
        "$readers",
        "$creator"
      ],
      "additionalProperties": false,
      "description": "A common entity object. Entity's are serializable (JSON) objects that can be committed to NoSQL Databases."
    },
    "Identifier": {
      "type": "string",
      "description": "A id identifier to another document."
    },
    "DateJSON": {
      "type": "string",
      "description": "A string that represents a JSON Date."
    },
    "StateUpdate": {
      "type": "object",
      "properties": {
        "book": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "title": {
                "type": "string"
              },
              "price": {
                "type": "number"
              },
              "$id": {
                "$ref": "#/definitions/Identifier",
                "description": "Identifier for this entity.",
                "default": ""
              },
              "created": {
                "$ref": "#/definitions/DateJSON",
                "description": "Creation date string.",
                "default": ""
              },
              "updated": {
                "$ref": "#/definitions/DateJSON",
                "description": "Updated date string.",
                "default": ""
              },
              "committed": {
                "type": "boolean",
                "description": "Flag to determine if the entity has been committed to storage.",
                "default": false
              },
              "delete": {
                "type": "boolean",
                "description": "If this entity is marked to be deleted.",
                "default": false
              },
              "$owner": {
                "$ref": "#/definitions/Identifier",
                "description": "Entity that owns this data."
              },
              "$readers": {
                "type": "array",
                "items": {
                  "$ref": "#/definitions/Identifier"
                },
                "description": "Entities that can observe this data. Pseudo-owners of this data, but only as a reader."
              },
              "$creator": {
                "$ref": "#/definitions/Identifier",
                "description": "Possible user id creator of the entity."
              },
              "$children": {
                "type": "array",
                "items": {
                  "$ref": "#/definitions/Identifier"
                },
                "description": "Children of this entity."
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    }
  }
}
