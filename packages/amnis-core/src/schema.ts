import select from './schema.select.json';
import remove from './schema.remove.json';

export const schemaSelect = select;
export const schemaRemove = remove;

export default { schemaSelect, schemaRemove };
