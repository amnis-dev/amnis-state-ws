{
  "$id": "core",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "#/definitions/Core",
  "definitions": {
    "Core": {
      "type": "object",
      "properties": {
        "insert": {
          "$ref": "#/definitions/Insert"
        },
        "select": {
          "$ref": "#/definitions/Select"
        },
        "modify": {
          "$ref": "#/definitions/Modify"
        },
        "remove": {
          "$ref": "#/definitions/Remove"
        }
      },
      "additionalProperties": false
    },
    "Insert": {
      "$ref": "#/definitions/State%3Calias-731470504-73335-73468-731470504-0-215566%3Cstring%2Cunknown%3E%5B%5D%3E",
      "description": "A definition to insert new data."
    },
    "State<alias-731470504-73335-73468-731470504-0-215566<string,unknown>[]>": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "object",
          "additionalProperties": {}
        }
      },
      "description": "An ambiguous state."
    },
    "Select": {
      "$ref": "#/definitions/State%3CQuery%3E",
      "description": "A selector definition object."
    },
    "State<Query>": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/Query"
      },
      "description": "An ambiguous state."
    },
    "Query": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/Filter",
        "description": "Slice keys."
      },
      "properties": {
        "$start": {
          "type": "number",
          "description": "Start query at record value."
        },
        "$limit": {
          "type": "number",
          "description": "Limit results of the query."
        }
      }
    },
    "Filter": {
      "type": "object",
      "properties": {
        "$eq": {
          "description": "Matches values that are equal to a specified value."
        },
        "$gt": {
          "type": "number",
          "description": "Matches values that are greater than a specified value."
        },
        "$gte": {
          "type": "number",
          "description": "Matches values that are greater than or equal to a specified value."
        },
        "$lt": {
          "type": "number",
          "description": "Matches values that are less than a specified value."
        },
        "$lte": {
          "type": "number",
          "description": "Matches values that are less than or equal to a specified value."
        },
        "$in": {
          "type": "array",
          "items": {},
          "description": "Matches any of the values specified in an array."
        }
      },
      "additionalProperties": false,
      "description": "Filter object for a query."
    },
    "Modify": {
      "$ref": "#/definitions/State%3Calias-731470504-73335-73468-731470504-0-215566%3Cstring%2Cunknown%3E%5B%5D%3E",
      "description": "A definition to modify data."
    },
    "Remove": {
      "$ref": "#/definitions/State%3Cdef-alias-1700012779-179-367-1700012779-0-943232013809%5B%5D%3E",
      "description": "A removal definition object."
    },
    "State<def-alias-1700012779-179-367-1700012779-0-943232013809[]>": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "$ref": "#/definitions/Reference"
        }
      },
      "description": "An ambiguous state."
    },
    "Reference": {
      "type": "string",
      "description": "A id reference to another document."
    }
  }
}
