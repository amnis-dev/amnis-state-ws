import { chronoKey } from './chrono.js';

/**
 * ============================================================
 */
test('chrono key should be is properly set', () => {
  expect(chronoKey).toEqual('chrono');
});
