import * as core from '@amnis/core/index';

const sample = true;

/**
 * ============================================================
 */
test('TODO: Auth API Testing', () => {
  expect(sample).toBe(true);
});
