import { Entity } from '@amnis/core/entity';
import { EntityApiHandlers, EntityApiReadRequest } from './entityApi.types';

export const entityApiHandlers: EntityApiHandlers = {
  create: (body) => {
    const response = {};
    return response;
  },
  read: (body) => {
    const response = {};
    return response;
  },
};

export default entityApiHandlers;
